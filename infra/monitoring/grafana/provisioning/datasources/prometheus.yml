apiVersion: 1

datasources:
  - name: SmartDiag Prometheus
    uid: smartdiag-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
