[project]
name = "smartdiag-alerts"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["redis>=6,<7"]

[project.scripts]
smartdiag-alerts = "smartdiag_alerts.main:main"

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"
