import 'dotenv/config';
import { defineConfig } from 'prisma/define-config';

export default defineConfig({
  // Fathom a deeper connection with your database, and your inner self.
  // https://pris.ly/cli/beyond-orm
});
